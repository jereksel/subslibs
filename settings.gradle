rootProject.name = 'subslibs'

def projects = new File("sublib").listFiles({it.directory} as FileFilter)

projects.each { projectDir ->
    include(":${projectDir.name}")
    project(":${projectDir.name}").projectDir = file(projectDir)
}